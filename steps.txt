0. Creating & setting up canvas element
1. Get rendering context
    a. make sure its valid (error checking)
2. Create & setup shaders
    a. create shader
    b. specify source
    c. compile
    d. error checking
3. Create & setup program
    a. create program
    b. attach shaders
    c. link program
    d. error checking
    e. use program
    f. ? validate program
4. Setup attributes & uniforms
    a. get attribute locations
    b. enable attributes
    c. get uniforms locations
5. Create & setup buffer
    a. create buffer
    b. bind buffer
    c. setup buffer data
        i. create data
        ii. pass in data to buffer
    d. setup buffer/data layout
        i. vertex position data layout
        ii. vertex color data layout
6. Setup viewport
    a. tranform positions from clip space to pixel space
7. Draw
    a. clear canvas
    b. draw